# Prompt Template: Refresh `AGENTS.md` (Antigravity)

**Objective:**
Ensure `AGENTS.md` reflects current development practices, tools (`.venv`), and project structure.

**Preparation:**
1. Check existing `AGENTS.md` files.
2. Review recent changes.

**Execution Steps:**
1. Open `AGENTS.md`.
2. Update outdated steps/commands (e.g., ensure `source .venv/bin/activate` is used).
3. Add new guidance.
4. Remove stale instructions.
5. Create scoped `AGENTS.md` for new subdirectories if needed.

**Documentation Requirements:**
- Concise, explicit commands.
- Standard formatting.

**Deliverable:**
- Updated `AGENTS.md` (committed).
