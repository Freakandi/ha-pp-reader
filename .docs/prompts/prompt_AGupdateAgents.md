# Prompt Template: Refresh `AGENT_HANDBOOK.md` (Antigravity)

**Objective:**
Ensure `AGENT_HANDBOOK.md` reflects current development practices, tools (`.venv`), and project structure.

**Preparation:**
1. Check existing `AGENT_HANDBOOK.md` files.
2. Review recent changes.

**Execution Steps:**
1. Open `AGENT_HANDBOOK.md`.
2. Update outdated steps/commands (e.g., ensure `source .venv/bin/activate` is used).
3. Add new guidance.
4. Remove stale instructions.
5. Create scoped `AGENT_HANDBOOK.md` for new subdirectories if needed.

**Documentation Requirements:**
- Concise, explicit commands.
- Standard formatting.

**Deliverable:**
- Updated `AGENT_HANDBOOK.md` (committed).
